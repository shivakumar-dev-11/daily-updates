Data Types
1. User-defined Data Types

a. Array
b. Object
c. Functions

2. Pre-defined Data Types

a. String
b. Boolean
c. Numbers
d. Null / Undefined

Functions

Functions are a collection of statements used to perform a task.

Events

User interactions with the application are called events.

Examples:

onclick

onChange

onSubmit

onReset

Operators
1. Arithmetic Operators

+ , - , * , / , ++ , -- , etc

2. Logical Operators

&& , || , !

3. Comparison Operators

>= , <= , < , > , == , === , ? (ternary operator)

4. Assignment Operators

= , += , -= , /=


switch
if–else Syntax
if (user === loggedIn) {
  console.log('hello user');
} else {
  console.log('please login');
}

if (dark) {
  giveDark;
} else if (white) {
  giveWhite;
} else {
  default;
}

switch Statement

Syntax:

switch (value) {
  case 'some case': {
    doThis;
    break;
  }

  case 'some other case': {
    doThis;
    break;
  }

  case 'nothing': {
    // don't do anything
    break;
  }

  default: {
    stayInGroundFloor;
  }
}


1.7 Looping Statements

Looping statements execute a block of code repeatedly until the condition becomes false.

Types of loops:

for

while

do-while

for-in

1. For Loop
Syntax:
for (initialisation; condition; increment/decrement) {
  // logic statements
}

2. For-in Loop

for-in loop is used only to iterate through objects.

for, while, and do-while loops are generally used to iterate through arrays.

for-in loop iterates through object keys.

Difference:

Arrays → iterated based on index

Objects → iterated from starting key to ending key

Syntax:
for (var key in objectName) {
  objectName[key]; // returns the value of that key
}

Example: for-in loop with Object
const exampleObj = {
  x: 1,
  y: 2,
  z: 3
};

for (var key in exampleObj) {
  console.log(exampleObj[key]);
}